diff --git a/android/app/res/values/config.xml b/android/app/res/values/config.xml
index d31c625..6855ed1 100644
--- a/android/app/res/values/config.xml
+++ b/android/app/res/values/config.xml
@@ -111,7 +111,9 @@
     <integer name="a2dp_source_codec_priority_aac">2001</integer>
     <integer name="a2dp_source_codec_priority_aptx">3001</integer>
     <integer name="a2dp_source_codec_priority_aptx_hd">4001</integer>
-    <integer name="a2dp_source_codec_priority_ldac">5001</integer>
+    <!-- Ranked by max quality: LDAC (24/96, 990kbps) above aptX-Adaptive
+         (~420kbps) but below the LHDC family (vendor-optimized, 24/96+). -->
+    <integer name="a2dp_source_codec_priority_ldac">7501</integer>
     <integer name="a2dp_source_codec_priority_lhdcv2">8002</integer>
     <integer name="a2dp_source_codec_priority_lhdcv3">8003</integer>
     <integer name="a2dp_source_codec_priority_lhdcv5">8004</integer>
diff --git a/android/app/src/com/android/bluetooth/a2dp/A2dpCodecConfig.java b/android/app/src/com/android/bluetooth/a2dp/A2dpCodecConfig.java
index 9a5bfcb..a619a24 100644
--- a/android/app/src/com/android/bluetooth/a2dp/A2dpCodecConfig.java
+++ b/android/app/src/com/android/bluetooth/a2dp/A2dpCodecConfig.java
@@ -32,7 +32,6 @@ import android.util.Log;
 
 import com.android.bluetooth.R;
 import com.android.bluetooth.flags.Flags;
-import com.android.modules.utils.build.SdkLevel;
 
 import java.util.List;
 import java.util.Objects;
@@ -334,18 +333,14 @@ class A2dpCodecConfig {
             mA2dpSourceCodecPriorityLhdcv3 = value;
         }
 
-        // LHDCv5 codec is not supported on Android 16 Baklava, the audio framework
-        // will not recognize the codec type and default to SBC as default
-        // configuration, which is often offloaded. This configuration can be overridden
-        // by setting the system property for LHDCv5 codec priority, e.g. in case SBC
-        // is not offloaded on the platform.
-        value =
-                SystemProperties.getInt(
-                        "bluetooth.a2dp.source.lhdcv5_priority.config",
-                        SdkLevel.isAtLeastC()
-                                ? 5002
-                                : BluetoothCodecConfig.CODEC_PRIORITY_DISABLED);
-
+        try {
+            value =
+                    SystemProperties.getInt(
+                            "bluetooth.a2dp.source.lhdcv5_priority.config",
+                            resources.getInteger(R.integer.a2dp_source_codec_priority_lhdcv5));
+        } catch (NotFoundException e) {
+            value = BluetoothCodecConfig.CODEC_PRIORITY_DEFAULT;
+        }
         if ((value >= BluetoothCodecConfig.CODEC_PRIORITY_DISABLED)
                 && (value < BluetoothCodecConfig.CODEC_PRIORITY_HIGHEST)) {
             mA2dpSourceCodecPriorityLhdcv5 = value;
