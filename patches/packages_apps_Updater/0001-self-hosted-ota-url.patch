diff --git a/app/src/main/res/values/strings.xml b/app/src/main/res/values/strings.xml
index 0251230..12b18b5 100644
--- a/app/src/main/res/values/strings.xml
+++ b/app/src/main/res/values/strings.xml
@@ -21,8 +21,15 @@
     <!--
         Optional placeholders replaced at runtime:
           {device} - Device name
+
+        Pointed at this build's own OTA index, not crDroid's. onyx has official
+        crDroid support, so the stock URL would offer official weeklies as
+        updates to an unofficial build and flash away the whole local patch set,
+        the PixelOS GApps and the Kono-Ha kernel. The file this points at is
+        generated by vendor/lineage/build/tools/createjson.sh at bacon time and
+        is an empty response array until a build is actually published there.
     -->
-    <string name="updater_server_url" translatable="false">https://raw.githubusercontent.com/crdroidandroid/android_vendor_crDroidOTA/16.0/{device}.json</string>
+    <string name="updater_server_url" translatable="false">https://raw.githubusercontent.com/Loukious/crdroid_onyx_patches/crdroid-16.0/ota/{device}.json</string>
 
     <string name="verification_failed_notification">Verification failed</string>
     <string name="verifying_download_notification">Verifying update</string>
